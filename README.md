# homebrew-lyrsmith

Homebrew tap for [lyrsmith](https://github.com/triluch/lyrsmith) — a TUI app for transcribing and editing synced song lyrics.

## Install

```
brew tap triluch/lyrsmith
brew install lyrsmith
```

> **Note:** `libmpv` is required and provided by Homebrew's `mpv` formula.
